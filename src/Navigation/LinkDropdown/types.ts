import { Link } from "../types";

export type LinkDropdownProps = {
  items: Link[];
  open?: boolean;
  className?: string;
  anchorEl?: any;
  onMouseEnter: any;
  onMouseLeave: any;
  ref: any;
};
